type: object
discriminator:
  propertyName: type
  mapping:
    event: '#/components/schemas/event-condition'
    entity: '#/components/schemas/entity-condition'
required:
  - type
properties:
  type:
    type: string
    description: The type of workflow condition