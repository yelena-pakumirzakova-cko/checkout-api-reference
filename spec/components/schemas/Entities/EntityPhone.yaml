type: object
title: Phone
properties:
  number:
    type: string
    description: "The phone number. This must not contain non-numeric characters and can't contain only zeros (For US numbers: Cannot start with 0 or 1)"
    minLength: 8
    maxLength: 16
    example: 2345678910
