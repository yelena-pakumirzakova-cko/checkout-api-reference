post:
  servers:
    - url: https://flow.cko-qa.ckotech.co/workflows
      description: QA
  security:
    - OAuth:
      - flow
      - flow:reflow
  tags:
    - Reflows
  summary: Reflow by subject
  description: |
    Reflows the events associated with the subject id and triggers the actions of any workflows with matching conditions.
  parameters:
    - in: path
      name: subjectId
      schema:
        type: string
        pattern: ^sbj_[a-z0-9]{26}$
      required: true
      description: The transaction subject identifier e.g. payment, dispute etc. The events associated with these subjects will be reflow-ed.
      example: sbj_x5zm2po6kimubhlfitgt2mfwgi
  responses:
    '202':
      description: Event reflow initiated successfully        
      headers:
        Cko-Request-Id:
          schema:
            $ref: "#/components/headers/Cko-Request-Id"
        Cko-Version:
          schema:
            $ref: "#/components/headers/Cko-Version"    
    '401':
      description: Unauthorized
    '404':
      description: Events for reflow not found
    '422':
      description: Invalid data was sent
      content:
        application/json:
         schema:
           $ref: '#/components/schemas/ValidationError'
    '500':
      description: Internal Error
