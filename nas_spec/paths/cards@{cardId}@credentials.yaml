get:
  tags:
    - Cards
  summary: Get the card credentials
  description: Returns the card credentials.
  servers:
    - url: https://api.checkout.com/issuing
      description: Live API
    - url: https://api.sandbox.checkout.com/issuing
      description: Sandbox API
  parameters:
    - in: path
      name: cardId
      required: true
      schema:
        $ref: '#/components/schemas/IssuingCardId'
    - in: query
      name: credentials
      required: false
      schema:
        type: string
        enum:
          - number
          - cvc2
        example: number,cvc2
        description: | 
          One or more comma-separated credential fields. 
          This works like a logical *AND* operator and returns the combined credentials in the response.
          Defaults to no credentials being returned.
  responses:
    '200':
      description: Card credentials returned successfully
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Credentials'   
    '401':
      description: Unauthorized
    '404':
      description: Card Not found
    '422':
      description: Invalid data was sent
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/IssuingValidationError'
    '500':
      description: Internal Server Error